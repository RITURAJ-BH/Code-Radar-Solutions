#include <stdio.h>
int main(){
    int n;
    scanf("%d",&n);
    int arr[n];
    for(int i=0;i<n;i++)scanf("%d ",&arr[i]);
        if(arr[int i]>arr[n-i]){
            printf("Sorted");
        }
        else{printf("Not Sorted");}
    }
