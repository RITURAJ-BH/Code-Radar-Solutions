#include <stdio.h>
int main(){
    int num;
    scanf("%d",&num);   
    int even,odd;
    int arr[num];
    for(int i=0;i<num;i++)scanf("%d",&arr[num]);
    for(int i=0;i<n;i++){
        if(arr[i]/2==0){
            even=+1;
        }
        else {odd=+1;}
    }
    printf("%d %d",even,odd);
}